@model IEnumerable<Neshagostar.DAL.DataModel.StorageRelated.OrdersRelated.SendingAllocated>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AllocatedProduct.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AmountSent)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AllocatedProduct.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AmountSent)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
